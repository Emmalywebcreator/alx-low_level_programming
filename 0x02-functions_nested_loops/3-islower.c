#include <stdio.h>
#include "main.h"

/**
 * _islowwer - check wheater char is a lowercase
 *
 * Return: 0 or 1
 */

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (0);
	}
	
	else
	{
		return (0);
	}
}
	
